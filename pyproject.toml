[project]
name = "mysite"
version = "0.1.0"
description = ""
authors = [
    {name = "Elizaveta",email = "liza1999malysheva@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django (==4.2.0)",
    "djangorestframework (>=3.14.0,<3.15.0)",  # Идеально совместим с Django 4.2
    "django-filter (>=23.0,<24.0)",          # Версия без конфликтов ClearableInput для Django 4.2
    "gunicorn (>=21.2.0,<22.0.0)"            # Стабильная версия для этого окружения
]


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
